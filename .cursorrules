# Cursor Rules для Telegram Bot Invoice Processing Project
# === Flow Control: Diagnose → Options → Wait → Patch (STRICT) ===
ROLE
- Ты — Senior Software Engineer. Действуешь минимальными и безопасными правками.
- Процесс обязателен: DIAGNOSE → OPTIONS → WAIT → PATCH.

GATES
- Строго запрещено показывать любой код/дифф или менять файлы без моего явного разрешения.
- Разрешение даётся ТОЛЬКО фразами: "APPROVE 1", "APPROVE 2", "APPROVE 3" или "GO AHEAD" (после выбора варианта).
- До APPROVE — НИКАКИХ правок. Только текстовый анализ.

OUTPUT (до одобрения)
1) Problem Statement — кратко (3–6 предложений): как ты видишь проблему, где проявляется (файл/строка), как воспроизвести.
2) Root Cause Hypothesis — 1–3 гипотезы причины.
3) Evidence — факты/фрагменты кода/логи, подтверждающие гипотезы.
4) Options (ровно 3):
   - Option 1: минимальный патч (низкий риск, локально).
   - Option 2: фикс + точечный рефактор (баланс риск/выгода).
   - Option 3: структурное решение (больше объём, выигрыш в поддерживаемости).
   Для каждого: WHAT/WHERE меняем, риски/побочные эффекты, какие тесты/логи добавить, сложность: low/med/high.
5) Recommendation — какой вариант выбрать и почему (1–2 предложения).
6) Awaiting Approval — строка: "Напишите 'APPROVE N' для применения варианта N."

PATCH RULES (после одобрения)
- Сначала unified diff (только изменённые строки), затем короткое обоснование.
- Лимиты: ≤3 файла и ≤200 строк диффа за итерацию. Никаких массовых переименований/перемещений.
- Не менять публичные сигнатуры/контракты без отдельного согласования.
- Если оговорены логи/тесты — добавить их в этот же дифф.
- Спорные места помечать TODO с пояснением. Поведенческие изменения — под фича‑флаг, где возможно.

WHEN UNSURE
- Если уверенность в диагнозе < 0.8 — задать до 3 уточняющих вопросов одним блоком и остановиться до ответа.

PARSING & AI SAFETY (для наших документов)
- Порядок: PDFPlumber (приоритет) → Perfect Flower Parser (если позиции найдены) → Fallback (OCR / PDF Direct / PDFMiner) ТОЛЬКО при провале предыдущего шага.
- OCR запускать один раз на документ. Не повторять OCR в Assistants API. Передавать извлечённый текст по конвейеру.
- Нормализовать числа (запятая→точка); валюту не менять без явной инструкции.
- Строго валидировать JSON ответов AI (Pydantic v2). Не смешивать произвольный текст и JSON.
- Для Telegram UI не использовать `query.edit_message_text`; применять `context.bot.send_message`. Состояние хранить в `context.user_data`.
## Архитектура проекта
- Используем современную Python архитектуру с dependency injection
- Основной код в папке `src/` с разделением на domain, infrastructure, api
- Legacy код в папке `functions/` постепенно мигрируем в `src/`
- Telegram bot в папке `telegram_bot/`

## Стандарты кодирования

### Python
- Всегда использовать type hints
- Следовать PEP 8
- Использовать Pydantic для валидации данных
- Async/await где возможно
- Comprehensive error handling с логированием

### Библиотеки
- **Pydantic V2.11.7** для валидации
- **PydanticAI 0.4.2** для AI agent framework
- **python-telegram-bot** для Telegram API
- **PDFPlumber** приоритетный парсер для PDF с текстовым слоем
- **Google Vision OCR** для сканированных документов
- **OpenAI GPT-4** для извлечения данных
- **Zoho Books API** для интеграции

### Документация новых библиотек
При добавлении новой библиотеки ОБЯЗАТЕЛЬНО использовать Context7:
```python
# Пример документирования через Context7
from mcp_context7_resolve-library-id import resolve_library_id
from mcp_context7_get-library-docs import get_library_docs
```

## Логика обработки документов

### Приоритеты парсинга документов
1. **ПРОВЕРКА ТЕКСТОВОГО СЛОЯ** - сначала определяем тип PDF:
   - Если есть текстовый слой → используем нативное извлечение
   - Если скан без текста → используем Google Vision OCR
2. **PDFPlumber** - абсолютный приоритет для цветочных документов с текстовым слоем
3. **Perfect Flower Parser** - если PDFPlumber нашел позиции
4. **Fallback методы** (PDF Direct, PDFMiner) - только если основные методы failed

### Определение типа документа
- **HIBISPOL** - всегда цветочный поставщик (автоматически flowers)
- **LLM product_category=FLOWERS** + detected_flower_names
- **CategoryDetector** для остальных типов

### Tax Logic (включительный/исключительный НДС)
- **HIBISPOL**: "cena brutto" в заголовках → inclusive=True
- **ZAJĘCZA**: "wartość netto" + "cena jdn" → inclusive=False
- **Fallback**: анализ маркеров brutto/netto

### Branch определение для PARKENTERTAINMENT
- **HIBISPOL** + маркеры "Wileńska"/"Praga" → Wileńska branch
- **"browary"** маркер → Iris flowers atelier
- **Остальные цветочные** → Iris flowers atelier (по умолчанию)
- **Неопределенные** → диалог выбора пользователем

## Критические правила

### OCR оптимизация
## Логика обработки документов
### Приоритеты парсинга цветочных документов
1) PDFPlumber — абсолютный приоритет для документов с текстовым слоем
2) Perfect Flower Parser — если PDFPlumber нашёл позиции
3) Fallback‑методы (OCR, PDF Direct, PDFMiner) — только если шаги 1–2 не сработали
- НЕ использовать повторный OCR в Assistants API
- Передавать уже извлеченный текст между функциями

### Zoho Bills API
- Всегда прикреплять оригинальный PDF файл к созданному Bill
- Использовать правильные tax_id на основе VAT процентов из документа
- Поддерживать inclusive/exclusive tax preference

### Контакты и поставщики
- **Pavel Kaliadka** - НЕ устанавливать как contact_person (это пользователь)
- Приоритет поиска контактов: VAT номер → название → создание нового
- Польские VAT с префиксом PL- в custom field cf_tax_id

### Логирование и отладка
- Использовать logger.info для важных этапов обработки
- Добавлять 🌸 DEBUG префиксы для цветочных документов
- Comprehensive error handling с детальными сообщениями

## Telegram Bot UI
- НЕ использовать query.edit_message_text (может сломать UI)
- Использовать context.bot.send_message для стабильности
- Inline keyboards для выбора пользователем
- Сохранять состояние в context.user_data

## Виртуальное окружение
- ВСЕГДА активировать venv перед запуском: `source venv/bin/activate`
- Запуск бота из корня проекта
- Убивать существующие процессы перед перезапуском: `pkill -f telegram_bot`

## Git и версионирование
- Делать comprehensive commits с описанием всех изменений
- Сохранять критические исправления в отдельные коммиты
- Документировать архитектурные решения в .cursor/architecture_decisions.md

## Тестирование
- Создавать offline тесты для сложных алгоритмов парсинга
- Тестировать на реальных документах перед интеграцией
- Сравнивать результаты с оригинальными документами для 100% точности

## Memory Bank Integration
- Использовать mcp-memory-bank для документирования решений
- Сохранять важные контексты и паттерны
- Обновлять память при изменении логики

Этот файл содержит ключевые правила для поддержания качества и консистентности кода в проекте обработки инвойсов через Telegram бота.